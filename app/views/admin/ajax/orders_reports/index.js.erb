$("#reports-result-info").html("<%= j render 'admin/reports/detail_info', total_price: @total_price, total_order: @total_order, total_products: @total_products %>");
$("#reports-result-table").html("<%= j render 'admin/reports/table_info', order_of_day: @order_of_day %>");
$("#charts-info").html("<%= j render 'admin/reports/charts_info', report_selling_product: @report_selling_product %>");

$('#table_report_day').DataTable();


var data = $("#chartsproduct").data("reportsproduct");
Highcharts.chart('chartsproduct', {
  chart: {
      type: 'column'
  },
  title: {
      text: '15 sản phẩm bán chạy nhất trong ngày'
  },
  xAxis: {
      type: 'category',
      labels: {
          rotation: -45,
          style: {
              fontSize: '13px',
              fontFamily: 'Verdana, sans-serif'
          }
      }
  },
  yAxis: {
      min: 0,
      title: {
          text: 'Số Lượng Bán Ra'
      }
  },
  legend: {
      enabled: false
  },
  tooltip: {
      pointFormat: 'Bán được: <b>{point.y} Sản Phẩm</b>'
  },
  series: [{
      name: 'Population',
      data: data,
      dataLabels: {
          enabled: true,
          rotation: -90,
          color: '#FFFFFF',
          align: 'right',
          format: '{point.y} Sản Phẩm', // one decimal
          y: 10, // 10 pixels down from the top
          style: {
              fontSize: '13px',
              fontFamily: 'Verdana, sans-serif'
          }
      }
    }]
  });
